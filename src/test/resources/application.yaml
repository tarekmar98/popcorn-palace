server:
  port: 8080

spring:
  application:
    name: popcorn-palace-test
  datasource:
    url: jdbc:postgresql://localhost:54321/popcorn-palace-test
    username: popcorn-palace-test
    password: popcorn-palace-test
    driverClassName: org.postgresql.Driver
    platform: postgres
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always